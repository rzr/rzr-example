# -*- mode: python; python-indent-offset: 4; indent-tabs-mode: nil -*-

#working
SConscript('src/SConscript')

#Failing:
#SConscript('src/SConscript', variant_dir='build', duplicate=0)
#SConscript('src/SConscript', variant_dir='build', build_dir='build')
#SConscript('src/SConscript', variant_dir='build/src', build_dir='build/src', duplicate=0)
#VariantDir('build/src', 'src', duplicate=1)
#VariantDir('build/src', 'src')
#SConscript('build/src/SConscript')
#SConscript('src/SConscript', variant_dir='build/src', duplicate=0)

